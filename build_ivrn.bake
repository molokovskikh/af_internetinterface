import file from Web.bake
import file from Deploy.bake

Global(
	HumanReadableName : "Сайт интернет провайдера",
	Project : @InforoomInternet,
	DeployAlias : @IVrn,
	JenkinsProject: @InternetInterface
)

task @default, [@Build]

task @Build, [@BuildWebSite]

task @deploy, [@DeployPipeline, @CopyTiny]
