import file from Migration.bake
import file from Deploy.bake
import file from WebSiteSupport.bake
import file from Tools.bake

Global(
	HumanReadableName : "Сайт интернет провайдера",
	Project : @InforoomInternet,
	DeployAlias : @IVrn
)
task @default, [@Build]

task @Build, [@BuildWebSite]

task @deploy, [@CheckScmChanges, @BuildWebSite, @Production, @Migrate, @Backup, @WebDeploy, @SendDeployNotification]
