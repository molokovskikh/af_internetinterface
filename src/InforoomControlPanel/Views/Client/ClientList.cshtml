@using Inforoom2.Components 
@using Inforoom2.Models
@model dynamic

@{
	ViewBag.Title = "Список пользователей";
	Layout = "~/Views/Shared/_Layout.cshtml";
	IList<Client> clients = ViewBag.Clients;
	ModelFilter<Client> pager = ViewBag.Pager;
}
<div id="table-2_wrapper" class="dataTables_wrapper form-inline no-footer">
	<table id="table-2" class="table table-bordered table-striped datatable dataTable no-footer" role="grid" aria-describedby="table-2_info">
		<thead>
			<tr role="row">
				<th class="sorting_asc" rowspan="1" colspan="1" style="width: 50px;" aria-label="">
					<div class="checkbox checkbox-replace neon-cb-replacement">
						<label class="cb-wrapper">
							<input type="checkbox" id="chk-1">
							<div class="checked"></div>
						</label>
					</div>
				</th>
				<th class="sorting" tabindex="0" aria-controls="table-2"><a href="@pager.OrderBy(s => s.Id)">Номер</a></th>
				<th class="sorting" tabindex="0" aria-controls="table-2"><a href="@pager.OrderBy(s => s.PhysicalClient.Name)">Имя</a></th>
				<th class="sorting" tabindex="0" aria-controls="table-2">Действия</th>
			</tr>
		</thead>

		<tbody>
			@foreach (var client in clients)
			{
				<tr role="row" class="odd">
					<td class="sorting_1">
						<div class="checkbox checkbox-replace neon-cb-replacement">
							<label class="cb-wrapper"><input type="checkbox" id="chk-1"><div class="checked"></div></label>
						</div>
					</td>
					<td><a href="@Url.Action("ClientInfo", "Client", new { clientId = client.Id })">@client.Id</a></td>
					<td><a href="@Url.Action("ClientInfo", "Client", new { clientId = client.Id })">@client.PhysicalClient.FullName</a></td>
					<td>
						<a class="btn btn-success btn-sm btn-icon icon-left" href="@Url.Action("ServiceRequest", "Client", new { clientId = client.Id })">
							<i class="entypo-plus-circled"></i>
							Сервисная заявка
						</a>
						<a class="btn btn-success btn-sm btn-icon icon-left" href="@Url.Action("ConnectionRequest", "Client", new { clientId = client.Id })">
							<i class="entypo-plus-circled"></i>
							Заявка на подключение
						</a>
						<form style="display: inline" action="@Url.Action("ApplyImpersonation", "AdminAccount")" method="post" target="_blank">
							<input name="client.Id" type="hidden" value="@client.Id">
							<button class="btn btn-success btn-sm btn-icon icon-left" type="submit" value="Войти">
								<i class="entypo-key"></i>
								Войти в личный кабинет
							</button>
						</form>
					</td>
				</tr>
			}
		</tbody>
	</table>
	<div class="row">
		@{ Html.RenderPartial("Pagination"); }
	</div>
</div>
